/** Debe coincidir con backgroundColor del splash nativo en app.json. */
export const SPLASH_BACKGROUND = '#121660';

/** Pantalla 1: fondo navy (splash nativo). */
export const NATIVE_SPLASH_DURATION_MS = 600;

/** Pantalla 2: logo Odontica (overlay JS). */
export const BRAND_SPLASH_DURATION_MS = 1800;
